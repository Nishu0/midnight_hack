# postgres connection for the registry. matches the postgres service in
# ../docker-compose.yml. copy to .env if you need to override.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/nightpool
PORT=8787
